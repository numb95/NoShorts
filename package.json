{
  "name": "noshorts",
  "version": "1.0.0",
  "private": true,
  "description": "Remove YouTube Shorts elements everywhere on YouTube.",
  "type": "commonjs",
  "scripts": {
    "test": "npm run test:unit",
    "test:unit": "jest",
    "test:smoke": "playwright test",
    "playwright:install": "playwright install",
    "version:dev": "node scripts/version.mjs",
    "version:release": "node scripts/version.mjs --release",
    "test:firefox": "web-ext lint --source-dir .",
    "firefox:run": "web-ext run -t firefox-desktop --source-dir ."
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "jest": "^29.7.0",
    "jsdom": "^22.1.0",
    "web-ext": "^8.0.0"
  },
  "dependencies": {
    "jest-environment-jsdom": "^30.2.0"
  }
}
